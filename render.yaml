services:
  - type: web
    name: cheppu-ai-proxy
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: HF_TOKEN
        sync: false
    autoDeploy: true
